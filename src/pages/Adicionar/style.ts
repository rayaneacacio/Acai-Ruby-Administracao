import styled from "styled-components";

export const Container = styled.div`
  .divAdicionar {
    display: flex;
    align-items: flex-end;
    gap: 1rem;
    margin-bottom: 3rem;
  }
  
  .inputDiv {
    width: calc(100% - 5rem);
  }
`;